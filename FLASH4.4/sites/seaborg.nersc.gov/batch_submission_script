# LoadLeveler submission script for seaborg.nersc.gov
#
#@ job_name        = flash
#@ output          = flash.$(jobid).out
#@ error           = flash.$(jobid).err
#@ job_type        = parallel
#@ environment     = COPY_ALL
#@ notification    = complete
#@ network.MPI     = sn_all,not_shared,us
#@ node_usage      = not_shared
#@ class           = regular
#
#
#@ tasks_per_node  = 16
#@ node            = 1
#@ wall_clock_limit= 00:40:00
#
#@ queue

flash3
